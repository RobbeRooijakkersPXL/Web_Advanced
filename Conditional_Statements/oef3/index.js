const getal = prompt("Voer een getal in:");
const getalNum = Number(getal);

if (getalNum === 7) {
  alert("Je hebt het juiste getal ingevoerd!");
} else {
  alert("Helaas, dat is niet het juiste getal.");
}